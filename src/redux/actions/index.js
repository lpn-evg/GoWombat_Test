import { createAction } from 'redux-actions';
import * as actionTypes from '../actionTypes';

export const setData = createAction(actionTypes.SET_DATA);
export const changeData = createAction(actionTypes.CHANGE_DATA);









